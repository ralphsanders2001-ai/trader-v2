# Robinhood credentials template — copy to rh_credentials.py and fill in.
# rh_credentials.py is gitignored; NEVER commit real credentials.

ROBINHOOD_USERNAME = "your_email@example.com"
ROBINHOOD_PASSWORD = "your_password"